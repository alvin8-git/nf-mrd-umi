// Local fallback: conda env instead of Docker (HPC nodes without a container
// runtime). Reuses the env built for the validation runs. NOT the default --
// the docker profile is the tested path.

conda.enabled  = true
docker.enabled = false

process {
    conda = '/data/alvin/envs/mrd'
}
