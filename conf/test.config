// Minimal test profile: caps resources so the pipeline can run on a small box
// or CI. Inherit the docker profile's containers when combined: -profile test,docker

process {
    resourceLimits = [ cpus: 4, memory: 16.GB, time: 2.h ]
}

params {
    max_cpus   = 4
    max_memory = '16.GB'
}
